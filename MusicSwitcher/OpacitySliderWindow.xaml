<Window x:Class="MusicSwitcher.OpacitySliderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Прозрачность" Height="120" Width="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ValueText" Grid.Row="0" Margin="0,0,0,8" Text="100%"/>
        <Slider x:Name="OpacitySlider" Grid.Row="1" Minimum="30" Maximum="100" Value="100"
                ValueChanged="OpacitySlider_ValueChanged"/>
        <Button Grid.Row="2" Content="OK" HorizontalAlignment="Right" Margin="0,15,0,0"
                Padding="20,5" Click="Ok_Click"/>
    </Grid>
</Window>
